language: cpp

compiler:
  - gcc
  - clang

install:
  - sudo apt-get install root

script:
  - source nptool/nptool.sh
  - npl
  - cmake ./
  - make install
  
branches:
  only:
    - NPTool.2.dev

os:
  - linux
